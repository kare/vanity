---
name: Release created
on:
  release:
    types:
      - created
jobs:
  released:
    uses: kare/github/.github/workflows/go-release.yaml@v0.5.0
    with:
      go_module_path: kkn.fi/vanity
      version: ${{ github.event.release.tag_name }}
